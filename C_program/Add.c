#include <stdio.h>

/**
 * main-calculate the sum of the value assign
 * to the variable
 * Return:0 as success
 *
 */
int main(void)
{
	int i = 500;
	int u = 500;
	int v = i + u;

	printf("total = %i\n", v);

	return (0); 
}
