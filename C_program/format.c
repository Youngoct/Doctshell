#include <stdio.h>

/**
 * main - print the format
 * Return: 0 as success
 */

int main(void)
{
	int m = 40;
	float k = 2.9765;
	char h = 'I';

	printf("value of m = %i\n", m);
	printf("value of k = %f\n", k);
	printf("value of h = %C\n", h);
	return (0);
}
